const dato = Buffer.from('Hello, World!'); // Convierte el string en binario, para optimizar el espacio en memoria que pudiera ocupar.
console.log(dato);

console.log(dato.toString()); // Convierte la información binaria en un string.